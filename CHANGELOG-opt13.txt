SUNNY opt13 — cloud resolution / fidelity
=========================================

Thresholds (NOW only; scrub/Play = GeoColor/WMS, hires hidden):
- Far z < 5.5: GOES GeoColor only
- Mid z ≥ 5.5: GeoColor + light MODIS/VIIRS (ramp t=(z-5.5)/2)
- IEM 1km from z ≥ 6 (opacity ~0.40→0.62 × cloudOpacity)
- Close z ≥ 6.2: ease GeoColor down (max ~42% reduce, floor 0.32×slider)
  so sharp layers read; never zero

Opacities (respect cloudOpacity slider; caps avoid burying basemap):
- MODIS: sharp×(0.40+0.32t), cap 0.88×slider
- VIIRS: sharp×(0.30+0.28t), cap 0.78×slider
- IEM: (0.40+0.22×iemT)×slider, max 0.62×slider

Play WMS max side (cached/debounced resize kept):
- phone ~960; tablet ~1280; desktop ≥1100 → 1600; wide+DPR≥1.5 → 2048

Raster resampling: linear below z8; nearest at z≥8 (setPaintProperty on
zoomend + debounced moveend).

Cache: ?v=opt13; SW CACHE=sunny-v-opt13
Preserved: opt11 layer order, Play overlay, beaches above clouds, SUNNY_CLOUD=20,
settings persist, FPS, PWA, tab-hidden pause, basemap helpers.
