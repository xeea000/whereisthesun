SUNNY opt21 — Loop Play + cover crossfade (no basemap dip)
==========================================================
A) Loop checkbox next to Play (default ON, sunny-loop).
   Play always restarts from index 0. Loop wraps to 0 at end;
   Loop off stops at end. Prefetch wraps when looping.
B) Cover fade: outgoing stays full opacity; only incoming
   fades 0→op on top. No mid-fade coverage dip / basemap flash.
   MapLibre static held until first play frame cover completes.
   #cloud-play-layer background transparent (was #000).
C) Radar: same loop + restart-from-0; setTiles keeps prior tiles.

Cache: ?v=opt21; SW CACHE=sunny-v-opt21
