SUNNY opt12 — sensible one-person pack
======================================

- Persist: basemap, cloud opacity, Labels, International, last map center+zoom
  (localStorage; restore after map ready; view save debounced on moveend)
- Prefetch: Play ahead tightened to next 1–2 frames; keep last-GPS 1–2 region warm
- Cache: BEACH_DB_VER → region1-bin1-idb4-opt12; SW CACHE=sunny-v-opt12; ?v=opt12
  Region IDB SWR kept; no Play frame IDB
- Weather: currentWxCache shared ~0.05° bins (Find / list / hover)
- Tab hidden: no cloud network / Play prefetch / hires; resume on visible; FPS pauses
- Debounce: moveend paint/regions ~220ms; Play bbox reload ~250ms
- Lists: SUNNY_PICK_CAP=12; sunny sheet reuses row DOM; popup reuses one instance
- PWA: manifest.webmanifest + icon-192/512; SW precaches; network-first shell
- FPS: #fps top-right near nav, ~11px muted, rAF ~4 Hz text update
- Preserved: opt11 layer order, Play image overlay, SUNNY_CLOUD=20, opaque high clouds,
  attribution bottom-right
