SUNNY opt22 — Play speed rate control
=====================================
A) Speed control next to Loop in .time-row: select #play-speed
   with 0.5x · 1x · 1.5x · 2x · 3x (default 1x).
   Persist sunny-play-speed in localStorage.
B) Base dwell stays PLAY_DWELL_MS / RADAR_PLAY_DWELL_MS = 900.
   Effective dwell = max(120, round(BASE / speed)).
   Cloud + radar Play schedules read live speed each tick.
   Retry holds stay fixed; cover fade duration unchanged.
C) Compact select styled to match timeline chrome; tighter on phone.

Cache: ?v=opt22; SW CACHE=sunny-v-opt22
